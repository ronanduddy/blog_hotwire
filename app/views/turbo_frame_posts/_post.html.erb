<%= turbo_frame_tag post do %>
  <div>
    <p>
      <strong>Title:</strong>
      <%= post.title %>
    </p>

    <p>
      <strong>Body:</strong>
      <%= post.body %>
    </p>

    <p>
      <%= "#{time_ago_in_words(post.created_at)} ago | #{post.blogger.email}" %>
    </p>
  </div>
<% end %>
