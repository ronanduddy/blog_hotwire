<%# commenting out the remove below as the model will broadcast its removal%>
<%#= turbo_stream.remove(dom_id(@post, "row")) %>

<%= turbo_stream.prepend("flash", partial: "/flash") %>

<%#= the following is no longer required any more now that the css only-child approach is being used %>
<%# <% unless @current_blogger.turbo_frame_posts.exists? %>
  <%#= turbo_stream.update(TurboFramePost.new, partial: "empty_state") %>
<%# end %>

